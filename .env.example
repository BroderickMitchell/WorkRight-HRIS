# Derived at deploy time; do not commit real secrets
PROJECT_ID=workright-hris
INSTANCE_CONNECTION_NAME=workright-hris:australia-southeast1:hris-db-7937
DB_NAME=workrightdb
DB_USER=appuser

# Computed by deploy script:
# DATABASE_URL=postgresql://appuser:${DB_PASSWORD}@/${DB_NAME}?host=/cloudsql/${INSTANCE_CONNECTION_NAME}&schema=public
