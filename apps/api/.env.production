# ============================================================
#  WorkRight HRIS - Production Environment Configuration
#  Cloud Run + Cloud SQL (PostgreSQL 14, IAM Authentication)
# ============================================================

# ---------- App ----------
NODE_ENV=production
PORT=8080
LOG_LEVEL=info

# ---------- Database ----------
# PostgreSQL 14 on Cloud SQL
# Using IAM service account authentication (no password required)
# Database name: todo
DATABASE_URL="postgresql://hris-dev-run-sa@workright-dev.iam@/todo?host=/cloudsql/workright-dev:australia-southeast1:hris-dev-db-d506&sslmode=disable"

# ---------- Prisma ----------
PRISMA_DATABASE_URL=${DATABASE_URL}
PRISMA_GENERATE_SKIP=false

# ---------- Authentication / Security ----------
# Generate this locally: openssl rand -base64 32
JWT_SECRET="your_generated_jwt_secret_here"

# ---------- API / Frontend ----------
# Replace these URLs with your live deployment endpoints
API_BASE_URL="https://your-api-service-name.a.run.app"
FRONTEND_URL="https://your-frontend-domain.com"

# ---------- Misc ----------
LOG_LEVEL=info

