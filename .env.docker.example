# Environment variables consumed by docker-compose.yml when starting local services.
# Copy to `.env.docker` and customise the credentials before running `docker compose up`.
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=workright
